import { Widget } from './widget.model';
export interface ILibraryWidget {
    isRendered: boolean;
    widget: Widget;
}
export declare class Library {
    private renderedWidgets;
    private availableWidgets;
    isDisplayed: boolean;
    xPosition: number;
    yPosition: number;
    widgets: ILibraryWidget[];
    constructor(renderedWidgets: Widget[], availableWidgets: Widget[]);
    private isWidgetRendered(widget);
    private buildLibrary();
    edit(xPosition: number, yPosition: number): void;
    hide(): void;
    renderWidget(widget: Widget): void;
    derenderWidget(widget: Widget): void;
}
