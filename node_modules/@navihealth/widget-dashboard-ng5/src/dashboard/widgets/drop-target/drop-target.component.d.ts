import { OnInit, ElementRef, AfterContentInit } from '@angular/core';
import { Widget } from '../widget.model';
import { BootstrapService } from '../bootstrap.service';
export declare class DropTargetComponent implements OnInit, AfterContentInit {
    private elementRef;
    private bootstrapService;
    dropTarget: Widget;
    height: number;
    constructor(elementRef: ElementRef, bootstrapService: BootstrapService);
    ngOnInit(): void;
    ngAfterContentInit(): void;
}
